// @generated by protoc-gen-connect-es v1.6.1 with parameter "target=ts"
// @generated from file tm/task_manager.proto (package vsmr.tm.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { Empty, MethodKind } from "@bufbuild/protobuf";
import { ListTasksByTypeRequest, SessionId, TaskArgs, TaskId, TaskInfoList, TaskTypeList } from "./task_manager_pb.js";

/**
 * ============================================================
 * TaskManager Service
 *
 * Responsibilities:
 * - Create and manage client sessions.
 * - Create tasks (provision K8s resources).
 * - Delete tasks (clean up K8s resources; requires STOPPED state).
 * - Provide discovery APIs for tasks and task types.
 * - Detect client disconnects via streaming RPC.
 * ============================================================
 *
 * @generated from service vsmr.tm.v1.TaskManager
 */
export const TaskManager = {
  typeName: "vsmr.tm.v1.TaskManager",
  methods: {
    /**
     * ----------------------------------------------------------
     * CreateSession
     *
     * Creates a new session and opens a long-lived stream used
     * to detect client liveness.
     * ----------------------------------------------------------
     *
     * @generated from rpc vsmr.tm.v1.TaskManager.CreateSession
     */
    createSession: {
      name: "CreateSession",
      I: Empty,
      O: SessionId,
      kind: MethodKind.ServerStreaming,
    },
    /**
     * ----------------------------------------------------------
     * CreateTask
     *
     * Creates a new task and provisions K8s resources.
     *
     * Semantics:
     * - Asynchronous and best-effort.
     * - Successful return means the creation request was accepted.
     * - No guarantee that the task is running or will run.
     * - When start_mode is AUTO (default), --auto-start is
     *   appended to the adapter command-line arguments.
     *
     * Context:
     * - session_id MUST be provided via gRPC metadata
     *   (e.g. "x-session-id").
     * ----------------------------------------------------------
     *
     * @generated from rpc vsmr.tm.v1.TaskManager.CreateTask
     */
    createTask: {
      name: "CreateTask",
      I: TaskArgs,
      O: TaskId,
      kind: MethodKind.Unary,
    },
    /**
     * ----------------------------------------------------------
     * DeleteTask
     *
     * Deletes a task and cleans up K8s resources.
     *
     * Precondition:
     * - The task MUST be in STOPPED state.
     * - If the task is not stopped, returns FAILED_PRECONDITION.
     * - Client must call MARSTaskControl.Stop first.
     *
     * Context:
     * - session_id MUST be provided via gRPC metadata
     *   (e.g. "x-session-id").
     * ----------------------------------------------------------
     *
     * @generated from rpc vsmr.tm.v1.TaskManager.DeleteTask
     */
    deleteTask: {
      name: "DeleteTask",
      I: TaskId,
      O: Empty,
      kind: MethodKind.Unary,
    },
    /**
     * ----------------------------------------------------------
     * ListAllTasks
     *
     * Returns a snapshot list of all tasks currently known to
     * the TaskManager.
     *
     * Semantics:
     * - Provides discovery and UI listing functionality.
     * - The returned list is a best-effort snapshot and may be
     *   stale immediately after being returned.
     * - Task lifecycle state is NOT guaranteed by this API.
     *
     * Use cases:
     * - UI task overview
     * - Debugging / introspection
     * ----------------------------------------------------------
     *
     * @generated from rpc vsmr.tm.v1.TaskManager.ListAllTasks
     */
    listAllTasks: {
      name: "ListAllTasks",
      I: Empty,
      O: TaskInfoList,
      kind: MethodKind.Unary,
    },
    /**
     * ----------------------------------------------------------
     * ListTaskTypes
     *
     * Returns all registered task types supported by the
     * TaskManager.
     *
     * Semantics:
     * - Task types represent logical engines or execution modes
     *   (e.g. "mars").
     * - The list may be static or dynamically extended depending
     *   on implementation.
     *
     * Use cases:
     * - UI task creation dialogs
     * - Validation of task_type values
     * ----------------------------------------------------------
     *
     * @generated from rpc vsmr.tm.v1.TaskManager.ListTaskTypes
     */
    listTaskTypes: {
      name: "ListTaskTypes",
      I: Empty,
      O: TaskTypeList,
      kind: MethodKind.Unary,
    },
    /**
     * ----------------------------------------------------------
     * ListTasksByType
     *
     * Returns a snapshot list of tasks filtered by task type.
     *
     * Semantics:
     * - Equivalent to filtering ListAllTasks by task_type.
     * - Snapshot is best-effort and not a real-time guarantee.
     *
     * Use cases:
     * - UI views grouped by task type
     * - Engine-specific task monitoring
     * ----------------------------------------------------------
     *
     * @generated from rpc vsmr.tm.v1.TaskManager.ListTasksByType
     */
    listTasksByType: {
      name: "ListTasksByType",
      I: ListTasksByTypeRequest,
      O: TaskInfoList,
      kind: MethodKind.Unary,
    },
  }
} as const;

